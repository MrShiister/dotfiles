chezmoi:template:missing-key=zero
{{- if and (not ( env "REMOTE_CONTAINERS" )) ( not ( env "CODESPACES")) -}}
# This is Git's per-user configuration file.
[user]
	name = mrshiister
	email = 31813485+MrShiister@users.noreply.github.com
	signingkey = {{ .signingkey }}
[commit]
	gpgsign = true
[core]
	editor = code
{{- end -}}